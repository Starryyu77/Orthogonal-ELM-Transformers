# BERT OELM + GPT OELM 扩展实验规划

> 规划版本: v2.0
> 规划日期: 2026-02-08
> 负责人: 张天禹
> 规划状态: 📝 待确认

---

## 实验目标

1. **Phase 1**: XNLI数据集验证 (BERT)
   - 测试BERT OELM在跨语言NLI任务上的表现
   - 对比Baseline和OELM-Freeze
   - 记录最佳准确率、训练时间、数据分布

2. **Phase 2**: GPT移植 (核心创新)
   - 将BERT的分头正交初始化方法移植到GPT架构
   - 解决之前GPT+全局正交失败的问题

3. **Phase 3**: GPT消融实验 (⚠️ 必须包含Baseline对比)
   - **核心要求**: 每个数据集都必须跑Baseline vs OELM-Freeze对比
   - 多数据集验证 (TinyStories, OpenWebText, WikiText-103)
   - 完整对比: Baseline vs OELM-Freeze vs OELM-Random

---

## Phase 1: XNLI实验 (BERT)

### 1.1 实验设计

| 配置项 | Baseline | OELM-Freeze | OELM-Random (Ablation) |
|--------|----------|-------------|----------------------|
| 模型 | bert-base-uncased | bert-base-uncased | bert-base-uncased |
| 冻结Q/K | ❌ | ✅ + 正交初始化 | ✅ + 随机初始化 |
| 学习率 | 2e-5 | 1e-4 | 1e-4 |
| Batch Size | 32 | 32 | 32 |
| Epochs | 3 | 3 | 3 |
| 数据集 | XNLI (en) | XNLI (en) | XNLI (en) |

### 1.2 数据集信息

**XNLI (Cross-lingual NLI)**:
- 训练集: 392,702 (使用MNLI训练集)
- 验证集: 2,490 (英语)
- 类别: 3 (entailment, neutral, contradiction)
- 语言: 支持15种语言 (本次先用英语en)

### 1.3 预期输出 (含详细时间记录)

```json
{
  "dataset": "xnli_en",
  "environment": {
    "gpu": "RTX A5000",
    "cuda_version": "12.2",
    "pytorch_version": "2.0.1",
    "gpu_temp_start": "65°C",
    "gpu_temp_end": "72°C"
  },
  "baseline": {
    "performance": {
      "best_accuracy": "TBD",
      "final_loss": "TBD",
      "epochs": 3
    },
    "timing": {
      "total_seconds": "TBD",
      "formatted": "TBD (hh:mm:ss)",
      "per_epoch_avg": "TBD",
      "per_step_avg": "TBD",
      "validation_total": "TBD"
    }
  },
  "oelm_freeze": {
    "performance": {
      "best_accuracy": "TBD",
      "final_loss": "TBD",
      "epochs": 3
    },
    "timing": {
      "total_seconds": "TBD",
      "formatted": "TBD (hh:mm:ss)",
      "per_epoch_avg": "TBD",
      "per_step_avg": "TBD",
      "validation_total": "TBD"
    },
    "parameter_efficiency": "87.1% trainable"
  },
  "comparison": {
    "accuracy_gap": "TBD",
    "time_analysis": {
      "total_gap_seconds": "TBD",
      "total_gap_percent": "TBD",
      "per_step_gap": "TBD",
      "speedup_ratio": "TBD"
    },
    "conclusion": "TBD"
  }
}
```

### 1.4 时间安排

- 准备时间: 30分钟 (修改train_bert.py支持XNLI)
- Baseline训练: ~4小时
- OELM训练: ~4小时
- 分析总结: 30分钟
- **总计: ~9小时**

---

## Phase 2: GPT OELM移植

### 2.1 技术挑战

**之前GPT+全局正交失败的原因**:
```
问题: 对GPT的Q/K进行全局QR分解
结果: PPL 4.14 → 4.94 (+19%)
原因: 全局正交破坏了多头结构
```

**解决方案 - 分头正交**:
```
方法: 将Q/K重塑为[num_heads, head_dim, hidden_dim]
      每个head独立QR分解
预期: 保留多头表达能力，同时实现正交性
```

### 2.2 移植方案

#### 2.2.1 代码修改点

**文件**: `gpt-oelm-project/models/modeling_oelm.py`

1. **修改初始化函数**:
```python
# 旧: 全局正交 (失败)
def apply_orthogonal_init_gpt(weight):
    q, r = torch.linalg.qr(weight)  # [768, 768] 全局QR

# 新: 分头正交 (BERT成功经验)
def apply_head_wise_orthogonal_gpt(weight, num_heads):
    # [hidden_dim, hidden_dim] → [num_heads, head_dim, hidden_dim]
    # 每个head独立QR
```

2. **修改冻结策略**:
```python
def freeze_gpt_qk_parameters(model, freeze_mode=True):
    """冻结GPT的Q/K投影"""
    for name, param in model.named_parameters():
        if 'attn.query' in name or 'attn.key' in name:
            param.requires_grad = not freeze_mode
```

3. **保持其他参数可训练**:
   - Value投影
   - FFN (Feed-Forward Network)
   - LayerNorm
   - 词嵌入 (可选)
   - 输出层

### 2.2.2 GPT架构适配

| 组件 | BERT | GPT | 处理方式 |
|------|------|-----|----------|
| Q/K权重 | `attention.self.query/key` | `attn.query/key` | 修改路径匹配 |
| Head数量 | 12 | 12 | 保持一致 |
| Hidden Dim | 768 | 768 | 保持一致 |
| 结构 | Encoder | Decoder | 注意因果掩码 |

### 2.3 实验配置

| 配置项 | Baseline | OELM-Freeze | OELM-Random |
|--------|----------|-------------|-------------|
| 模型 | GPT Medium-512 | GPT Medium-512 | GPT Medium-512 |
| d_model | 512 | 512 | 512 |
| n_layers | 6 | 6 | 6 |
| n_heads | 8 | 8 | 8 |
| 冻结Q/K | ❌ | ✅ 正交 | ✅ 随机 |
| 学习率 | 3e-4 | 1e-3 | 1e-3 |
| Batch Size | 8 | 8 | 8 |

---

## Phase 3: GPT消融实验 (⚠️ 必须包含Baseline对比)

### ⚠️ 核心要求: Baseline对比

**每个数据集必须完成以下对比实验**:
```
对于每个数据集:
├── Baseline (全参数可训练)          ← 必须
├── OELM-Freeze (正交初始化+冻结Q/K)  ← 必须
└── OELM-Random (随机初始化+冻结Q/K)  ← 消融必须
```

**为什么Baseline对比至关重要**:
1. **量化性能损失**: 明确OELM-Freeze vs全参数微调的差距
2. **验证方法有效性**: 证明正交初始化的价值
3. **与BERT实验一致**: 保持实验设计的一致性
4. **论文必需**: 审稿人要求的对比基准

### 3.1 数据集选择

| 数据集 | 任务 | 规模 | 目的 |
|--------|------|------|------|
| **TinyStories** | 故事生成 | 100K | 短文本生成能力 |
| **OpenWebText** | 文本生成 | 中等 | 长文本连贯性 |
| **WikiText-103** | 语言建模 | 大型 | 知识密集型任务 |

### 3.2 评估指标

**语言建模任务**:
- **Perplexity (PPL)**: 主要指标，越低越好
- **Loss**: 训练/验证损失
- **Token Accuracy**: 预测准确率

**对比基准 (严格)**:
```
目标: OELM-Freeze PPL ≤ Baseline PPL × 1.05 (5%以内)
必报: Baseline vs OELM-Freeze的精确差距
```

### 3.3 实验矩阵 (含Baseline)

| 实验ID | 数据集 | 方法 | 学习率 | 预期时间 | Baseline对比 |
|--------|--------|------|--------|----------|--------------|
| GPT-01 | TinyStories | **Baseline** | 3e-4 | 8h | ✅ 基准 |
| GPT-02 | TinyStories | OELM-Freeze | 1e-3 | 8h | ✅ vs GPT-01 |
| GPT-03 | TinyStories | OELM-Random | 1e-3 | 8h | ✅ vs GPT-01 |
| GPT-04 | OpenWebText | **Baseline** | 3e-4 | 12h | ✅ 基准 |
| GPT-05 | OpenWebText | OELM-Freeze | 1e-3 | 12h | ✅ vs GPT-04 |
| GPT-06 | WikiText-103 | **Baseline** | 3e-4 | 24h | ✅ 基准 |
| GPT-07 | WikiText-103 | OELM-Freeze | 1e-3 | 24h | ✅ vs GPT-06 |

**总计: 7个实验 (其中3个Baseline基准)，约92小时 (4天)**

### 3.4 对比记录要求

**每个数据集必须记录**:

#### 性能指标
| 指标 | Baseline | OELM-Freeze | OELM-Random | 差距分析 |
|------|----------|-------------|-------------|----------|
| Best PPL | | | | vs Baseline |
| Final Loss | | | | vs Baseline |
| Convergence | | | | Steps to target |
| Parameter Efficiency | 100% | 87.1% | 87.1% | Saved 12.9% |

#### ⚠️ 时间指标 (严格记录)
| 时间指标 | Baseline | OELM-Freeze | OELM-Random | 差距分析 |
|----------|----------|-------------|-------------|----------|
| **总训练时间** | | | | 时:分:秒 |
| **每epoch平均** | | | | 秒 |
| **每步平均** | | | | 毫秒 |
| **验证总耗时** | | | | 秒 |
| **GPU利用率** | | | | % |

**时间计算公式**:
```
时间节省 = (Baseline时间 - OELM时间) / Baseline时间 × 100%
每步差距 = OELM每步时间 - Baseline每步时间
加速比 = Baseline总时间 / OELM总时间
```

**示例输出格式**:
```json
{
  "dataset": "TinyStories",
  "model": "GPT-Medium-512",
  "comparison": {
    "baseline": {
      "best_ppl": 15.2,
      "trainable_params": "100% (82M)",
      "timing": {
        "total_seconds": 29700,
        "formatted": "8h 15min",
        "per_epoch_avg": 9900,
        "per_step_avg": 0.284,
        "validation_total": 320
      }
    },
    "oelm_freeze": {
      "best_ppl": 15.8,
      "trainable_params": "87.1% (71M)",
      "timing": {
        "total_seconds": 30420,
        "formatted": "8h 27min",
        "per_epoch_avg": 10140,
        "per_step_avg": 0.326,
        "validation_total": 340
      },
      "vs_baseline": {
        "ppl_gap": "+3.9%",
        "time_gap": "+2.4%",
        "per_step_gap": "+14.8%",
        "params_saved": "12.9%"
      }
    }
  }
}
```

---

## 完整实验对比矩阵

### BERT vs GPT 对比实验总览

| 架构 | 数据集 | 任务类型 | Baseline | OELM-Freeze | OELM-Random | 必做 |
|------|--------|----------|----------|-------------|-------------|------|
| **BERT** | SST-2 | 2分类 | ✅ 93.12% | ✅ 91.28% | ✅ 82.11% | 已完成 |
| **BERT** | MNLI | 3分类 | ✅ 83.44% | ✅ 82.23% | ⬜ | 已完成 |
| **BERT** | XNLI-en | 3分类跨语言 | ⏳ TBD | ⏳ TBD | ⬜ | **Phase 1** |
| **GPT** | TinyStories | 生成 | ⏳ TBD | ⏳ TBD | ⏳ TBD | **Phase 3** |
| **GPT** | OpenWebText | 生成 | ⏳ TBD | ⏳ TBD | ⬜ | **Phase 3** |
| **GPT** | WikiText-103 | 语言建模 | ⏳ TBD | ⏳ TBD | ⬜ | **Phase 3** |

**图例**: ✅ 已完成 | ⏳ 待执行 | ⬜ 可选

### 关键对比维度

**每个实验必须回答的问题**:
1. **性能差距**: OELM vs Baseline的具体数值差距
2. **参数效率**: 节省12.9%参数是否值得
3. **训练速度**: 时间差异是否在可接受范围
4. **正交性必要性**: OELM-Random vs OELM-Freeze的对比
5. **跨架构迁移**: BERT的成功能否复制到GPT

---

## 实验记录模板

### 记录格式

```markdown
## 实验 [ID]: [名称]

### 配置
- 数据集:
- 方法:
- 学习率:
- Batch Size:
- 硬件: GPU [x]

### 结果
| 指标 | Baseline | OELM | 差距 |
|------|----------|------|------|
| 最佳准确率/PPL | | | |
| **总训练时间** | | | |
| **每epoch时间** | | | |
| **每步时间** | | | |
| **验证总时间** | | | |
| 最终Loss | | | |
| 收敛速度 | | | |

### 关键发现
-

### 问题与解决
-
```

---

## ⏱️ 训练时间记录规范 (⚠️ 严格执行)

### 时间记录要求

**每个实验必须记录以下时间指标**：

| 时间指标 | 记录方法 | 精度要求 | 说明 |
|----------|----------|----------|------|
| **总训练时间** | `time.perf_counter()` | 秒级 | 从训练开始到结束 |
| **每epoch时间** | 内置计时 | 秒级 | 每个epoch耗时 |
| **每步时间** | CUDA同步计时 | 毫秒级 | 参考BERT实验方法 |
| **验证时间** | 单独计时 | 秒级 | 每次validation耗时 |
| **数据加载时间** | 可选 | 秒级 | 排除在训练时间外 |

### 时间记录代码模板

```python
import time
import torch

# 总时间记录
start_time = time.perf_counter()

for epoch in range(epochs):
    epoch_start = time.perf_counter()

    for step, batch in enumerate(dataloader):
        step_start = time.perf_counter()

        # CUDA同步 (确保准确计时)
        torch.cuda.synchronize()

        # 训练步骤
        loss = train_step(batch)

        torch.cuda.synchronize()
        step_time = time.perf_counter() - step_start

    epoch_time = time.perf_counter() - epoch_start
    print(f"Epoch {epoch}: {epoch_time:.2f}s")

total_time = time.perf_counter() - start_time
print(f"Total training time: {total_time:.2f}s ({total_time/3600:.2f}h)")
```

### 时间对比分析要求

**每个数据集必须生成时间对比表**：

| 指标 | Baseline | OELM-Freeze | OELM-Random | 分析 |
|------|----------|-------------|-------------|------|
| 总训练时间 | | | | 差距% |
| 每epoch平均 | | | | 稳定性 |
| 每步平均 | | | | 效率对比 |
| 验证总耗时 | | | | 额外开销 |
| GPU利用率 | | | | 资源效率 |

**时间效率计算公式**：
```
时间节省比例 = (Baseline时间 - OELM时间) / Baseline时间 × 100%
每步时间差距 = OELM每步时间 - Baseline每步时间
加速比 = Baseline总时间 / OELM总时间
```

### 时间记录文件格式

**JSON输出示例**：
```json
{
  "timing": {
    "baseline": {
      "total_seconds": 29400,
      "total_formatted": "8h 10m",
      "per_epoch_avg": 9800,
      "per_step_avg": 0.284,
      "validation_total": 320
    },
    "oelm_freeze": {
      "total_seconds": 30120,
      "total_formatted": "8h 22m",
      "per_epoch_avg": 10040,
      "per_step_avg": 0.326,
      "validation_total": 340
    },
    "comparison": {
      "time_difference_seconds": 720,
      "time_difference_percent": "+2.45%",
      "speedup_ratio": 0.976
    }
  }
}
```

### 影响因素记录

**每次实验必须记录环境信息**：
- GPU型号和驱动版本
- GPU温度 (训练前后)
- 是否共享GPU (其他用户进程)
- CUDA版本
- PyTorch版本
- 系统负载 (训练期间)

---

## 时间安排总览

| 阶段 | 任务 | 预计时间 | 截止日期 |
|------|------|----------|----------|
| **Phase 1** | XNLI实验 | 1天 | Day 1 |
| **Phase 2** | GPT移植开发 | 1天 | Day 2 |
| **Phase 3** | TinyStories实验 | 1天 | Day 3 |
| **Phase 3** | OpenWebText实验 | 2天 | Day 5 |
| **Phase 3** | WikiText实验 | 2天 | Day 7 |
| **总结** | 报告撰写 | 1天 | Day 8 |

**总计: 8天**

---

## 风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| GPT移植后性能仍然不佳 | 中 | 高 | 准备多种学习率方案，记录详细日志分析 |
| XNLI数据加载问题 | 低 | 中 | 提前测试数据加载，准备备用方案 |
| 训练时间超预期 | 中 | 中 | 优先完成TinyStories，其他可选 |
| GPU资源不足 | 低 | 高 | 使用tmux后台运行，错峰实验 |

---

## 预期成果

1. **XNLI结果**: 验证OELM在跨语言任务上的泛化能力
   - **必须包含**: XNLI Baseline vs OELM-Freeze详细对比数据

2. **GPT方法**: 成功将分头正交初始化应用于GPT
   - **核心验证**: GPT Baseline vs OELM-Freeze性能对比
   - **关键指标**: PPL差距控制在5%以内

3. **消融实验**: 证明正交性在生成任务中的必要性
   - **三重对比**: Baseline vs OELM-Freeze vs OELM-Random
   - **多数据集**: 至少2个数据集完成完整对比

4. **时间分析**: 详细的训练时间对比
   - **总时间对比**: Baseline vs OELM总训练时间差距
   - **每步效率**: 每步训练时间、CUDA同步计时
   - **时间效率**: 参数减少是否带来时间优势
   - **可视化**: 训练时间柱状图、每步时间曲线

5. **实验报告**: 完整的对比分析报告，支持论文写作
   - **对比表格**: BERT vs GPT跨架构对比 (性能+时间)
   - **统计显著性**: 多次运行的均值±标准差
   - **可视化**: 训练曲线、对比柱状图、时间效率图

---

## 确认清单

请确认以下内容后开始实验：

### 实验设计确认
- [ ] Phase 1 (XNLI) 实验计划确认
- [ ] Phase 2 (GPT移植) 技术方案确认
- [ ] Phase 3 (消融实验) 数据集和指标确认

### ⚠️ 核心要求确认 (Baseline对比)
- [ ] **XNLI必须包含Baseline对比** (理解并同意)
- [ ] **GPT每个数据集必须包含Baseline对比** (理解并同意)
- [ ] **所有对比必须记录精确数值差距** (理解并同意)

### ⏱️ 训练时间记录确认
- [ ] **严格记录总训练时间** (时:分:秒)
- [ ] **记录每epoch平均时间** (秒)
- [ ] **记录每步平均时间** (毫秒，CUDA同步)
- [ ] **记录验证总耗时** (秒)
- [ ] **记录GPU利用率和环境信息** (温度、负载等)
- [ ] **计算时间效率指标** (时间节省%、加速比)

### 资源与时间确认
- [ ] 时间安排可接受 (8天)
- [ ] GPU资源可用
- [ ] 优先级别确认 (哪些实验必须完成，哪些可选)

### 启动确认
**请回复"确认"或提出修改意见，我将立即开始执行 Phase 1 (XNLI实验)**

**请回复确认或修改意见，我将立即开始执行。**

---

## 附录: 时间记录汇总表

### 所有实验时间对比总览

| 阶段 | 实验ID | 数据集 | 方法 | 总时间 | 每步时间 | 与Baseline差距 | 状态 |
|------|--------|--------|------|--------|----------|----------------|------|
| **Phase 1** | XNLI-01 | XNLI-en | Baseline | ⏳ TBD | ⏳ TBD | - | ⏳ 待执行 |
| **Phase 1** | XNLI-02 | XNLI-en | OELM-Freeze | ⏳ TBD | ⏳ TBD | ⏳ TBD | ⏳ 待执行 |
| **Phase 3** | GPT-01 | TinyStories | Baseline | ⏳ TBD | ⏳ TBD | - | ⏳ 待执行 |
| **Phase 3** | GPT-02 | TinyStories | OELM-Freeze | ⏳ TBD | ⏳ TBD | ⏳ TBD | ⏳ 待执行 |
| **Phase 3** | GPT-03 | TinyStories | OELM-Random | ⏳ TBD | ⏳ TBD | ⏳ TBD | ⏳ 待执行 |
| **Phase 3** | GPT-04 | OpenWebText | Baseline | ⏳ TBD | ⏳ TBD | - | ⏳ 待执行 |
| **Phase 3** | GPT-05 | OpenWebText | OELM-Freeze | ⏳ TBD | ⏳ TBD | ⏳ TBD | ⏳ 待执行 |

### 已完成实验时间参考 (BERT)

| 实验 | 数据集 | 总时间 | 每步时间 | 备注 |
|------|--------|--------|----------|------|
| BERT Baseline | SST-2 | ~30min | 0.3218s | 6,315 steps |
| BERT OELM | SST-2 | ~35min | 0.3262s | 6,315 steps |
| BERT Baseline | MNLI | ~4h | - | 36,816 steps |
| BERT OELM | MNLI | ~4h | - | 36,816 steps |

**参考结论**: BERT实验中OELM比Baseline慢约1.4%，每步慢约0.004秒

---

**规划版本**: v2.1 (含严格时间记录要求)
**最后更新**: 2026-02-08
